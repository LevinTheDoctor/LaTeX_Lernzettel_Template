%% commands.tex — Eigene Umgebungen und Befehle
%% Alle Farben kommen aus config/colormeta.tex; nichts ist hart kodiert.
%% ──────────────────────────────────────────────────────────────────────────

%% ══════════════════════════════════════════════════════════════════════════
%% 1.  process  +  \step{text}
%%     Nummerierter Schritt-für-Schritt-Ablauf mit farbigem linken Rand.
%%
%%  Verwendung:
%%    \begin{process}
%%      \step{Erster Schritt}
%%      \step{Zweiter Schritt}
%%    \end{process}
%% ══════════════════════════════════════════════════════════════════════════
\newcounter{procstep}

\newenvironment{process}{%
  \setcounter{procstep}{0}%
  \begin{tcolorbox}[
    enhanced,
    breakable,
    boxrule         = 0pt,
    frame hidden,
    borderline west = {3pt}{0pt}{stepcolor},
    colback         = stepcolor!6!white,
    left            = 10pt,
    right           = 8pt,
    top             = 6pt,
    bottom          = 6pt,
    arc             = 3pt,
  ]%
}{%
  \end{tcolorbox}%
}

\newcommand{\step}[1]{%
  \stepcounter{procstep}%
  \vspace{4pt}%
  \noindent
  \colorbox{stepcolor}{%
    \color{white}\bfseries\small\hspace{3pt}\theprocstep\hspace{3pt}%
  }%
  \hspace{6pt}#1\par
}

%% ══════════════════════════════════════════════════════════════════════════
%% 2.  infobox[Titel]
%%     Farbige Infobox mit optionalem fettem Titel.
%%
%%  Verwendung:
%%    \begin{infobox}[Mein Titel] ... \end{infobox}
%%    \begin{infobox}             ... \end{infobox}   % ohne Titel
%% ══════════════════════════════════════════════════════════════════════════
\NewDocumentEnvironment{infobox}{O{}}{%
  \def\lzinfoboxtitle{#1}%
  \ifdefempty{\lzinfoboxtitle}{%
    %% ── Ohne Titel ──
    \begin{tcolorbox}[
      enhanced, breakable,
      colback  = infocolor,
      colframe = infoframe,
      boxrule  = 1pt,
      arc      = 4pt,
      left     = \boxinnersep,
      right    = \boxinnersep,
      top      = \boxinnersep,
      bottom   = \boxinnersep,
    ]%
  }{%
    %% ── Mit Titel ──
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = infocolor,
      colframe     = infoframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{\lzinfoboxtitle}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = infoframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = infoframe,
        colframe = infoframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 3.  warnbox[Titel]
%%     Warnbox / Tipp-Box mit optionalem fettem Titel.
%% ══════════════════════════════════════════════════════════════════════════
\NewDocumentEnvironment{warnbox}{O{}}{%
  \def\lzwarnboxtitle{#1}%
  \ifdefempty{\lzwarnboxtitle}{%
    %% ── Ohne Titel ──
    \begin{tcolorbox}[
      enhanced, breakable,
      colback  = warncolor,
      colframe = warnframe,
      boxrule  = 1pt,
      arc      = 4pt,
      left     = \boxinnersep,
      right    = \boxinnersep,
      top      = \boxinnersep,
      bottom   = \boxinnersep,
    ]%
  }{%
    %% ── Mit Titel ──
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = warncolor,
      colframe     = warnframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{\lzwarnboxtitle}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = warnframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = warnframe,
        colframe = warnframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 4.  procon  +  \pro{text}  \con{text}
%%     Zwei-Spalten-Gegenüberstellung Vorteile / Nachteile.
%%     Alle \pro{} und \con{} werden gesammelt und am Ende nebeneinander
%%     als zwei tcolorbox-Minipages gesetzt.
%%
%%  Verwendung:
%%    \begin{procon}
%%      \pro{Vorteil 1}
%%      \con{Nachteil 1}
%%    \end{procon}
%% ══════════════════════════════════════════════════════════════════════════
\newenvironment{procon}{%
  \def\lzproitems{}%
  \def\lzconitems{}%
  % \def statt \newcommand: kein Fehler beim mehrfachen Aufruf
  % ##1 = #1 auf zweiter Makro-Ebene innerhalb von \newenvironment
  \def\pro##1{\appto\lzproitems{\item ##1}}%
  \def\con##1{\appto\lzconitems{\item ##1}}%
}{%
  \par\smallskip\noindent
  \begin{minipage}[t]{0.475\linewidth}
    \begin{tcolorbox}[
      enhanced,
      colback      = procolor,
      colframe     = proframe,
      boxrule      = 1pt,
      arc          = 4pt,
      title        = {Vorteile},
      fonttitle    = \bfseries\color{white},
      colbacktitle = proframe,
      left = 6pt, right = 6pt, top = 4pt, bottom = 4pt,
    ]
      \begin{itemize}[leftmargin=*, nosep, topsep=0pt]
        \lzproitems
      \end{itemize}
    \end{tcolorbox}
  \end{minipage}%
  \hfill
  \begin{minipage}[t]{0.475\linewidth}
    \begin{tcolorbox}[
      enhanced,
      colback      = concolor,
      colframe     = conframe,
      boxrule      = 1pt,
      arc          = 4pt,
      title        = {Nachteile},
      fonttitle    = \bfseries\color{white},
      colbacktitle = conframe,
      left = 6pt, right = 6pt, top = 4pt, bottom = 4pt,
    ]
      \begin{itemize}[leftmargin=*, nosep, topsep=0pt]
        \lzconitems
      \end{itemize}
    \end{tcolorbox}
  \end{minipage}%
  \par\smallskip
}

%% ══════════════════════════════════════════════════════════════════════════
%% 5.  codebox
%%     Monospace-Block mit grauem Hintergrund (für kurze Ausschnitte).
%%     Für Syntax-Highlighting: listings-Paket nutzen (siehe packages.tex).
%% ══════════════════════════════════════════════════════════════════════════
\newenvironment{codebox}{%
  \begin{tcolorbox}[
    enhanced,
    breakable,
    colback  = codeblock,
    colframe = codeframe,
    boxrule  = 0.5pt,
    arc      = 3pt,
    left     = 8pt,
    right    = 8pt,
    top      = 6pt,
    bottom   = 6pt,
    fontupper = \ttfamily\small,
  ]%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 6.  \sidetables{Inhalt links}{Inhalt rechts}
%%     Zwei beliebige Inhalte (z. B. Tabellen, Listen) nebeneinander.
%% ══════════════════════════════════════════════════════════════════════════
\newcommand{\sidetables}[2]{%
  \par\smallskip\noindent
  \begin{minipage}[t]{0.475\linewidth}
    #1
  \end{minipage}%
  \hfill
  \begin{minipage}[t]{0.475\linewidth}
    #2
  \end{minipage}%
  \par\smallskip
}

%% ══════════════════════════════════════════════════════════════════════════
%% 7.  stdtable{Spaltendefinition}{Kopfzeile}
%%     Formatierte Tabelle mit farbiger Kopfzeile und alternierenden Zeilen.
%%     Spaltentypen: l r c  für Text; X für auto-breite Spalten; p{…} für
%%     feste Breite. X-Spalten sind durch tabularx möglich, weil \NewEnviron
%%     (environ-Paket) den Body komplett sammelt, bevor tabularx ihn scannt.
%%
%%  Verwendung:
%%    \begin{stdtable}{l X X}{Algorithmus & Kompl. & Notiz}
%%      Bubble Sort & $O(n^2)$ & Einfach \\
%%    \end{stdtable}
%% ══════════════════════════════════════════════════════════════════════════
\NewEnviron{stdtable}[2]{%
  \par\smallskip
  \rowcolors{2}{tablerowalt}{white}%
  \begin{center}%
  \begin{tabularx}{\linewidth}{#1}%
  \rowcolor{tableheader}%
  \color{tableheadertext}\bfseries #2\\[\jot]%
  \BODY
  \end{tabularx}%
  \end{center}%
  \rowcolors{1}{}{}%   Reset: verhindert Übertrag auf nachfolgende Tabellen
  \par\smallskip
}

%% ══════════════════════════════════════════════════════════════════════════
%% 8.  merksatz[Titel]
%%     Wichtiger Satz / Merkeintrag (Indigo).
%%
%%  Verwendung:
%%    \begin{merksatz}[Satz von Pythagoras]
%%      In einem rechtwinkligen Dreieck gilt: $a^2 + b^2 = c^2$.
%%    \end{merksatz}
%% ══════════════════════════════════════════════════════════════════════════
\NewDocumentEnvironment{merksatz}{O{}}{%
  \def\lzmerksatztitle{#1}%
  \ifdefempty{\lzmerksatztitle}{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback  = merksatzcolor,
      colframe = merksatzframe,
      boxrule  = 1pt,
      arc      = 4pt,
      left     = \boxinnersep,
      right    = \boxinnersep,
      top      = \boxinnersep,
      bottom   = \boxinnersep,
    ]%
  }{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = merksatzcolor,
      colframe     = merksatzframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{\lzmerksatztitle}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = merksatzframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = merksatzframe,
        colframe = merksatzframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 9.  beispiel[Titel]
%%     Beispiel-Box (Smaragdgrün).
%%
%%  Verwendung:
%%    \begin{beispiel}[Quicksort auf [3,1,4,1,5]]
%%      Pivot = 3, links: [1,1], rechts: [4,5] …
%%    \end{beispiel}
%% ══════════════════════════════════════════════════════════════════════════
\NewDocumentEnvironment{beispiel}{O{}}{%
  \def\lzbeispieltitle{#1}%
  \ifdefempty{\lzbeispieltitle}{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback  = beispielcolor,
      colframe = beispielframe,
      boxrule  = 1pt,
      arc      = 4pt,
      left     = \boxinnersep,
      right    = \boxinnersep,
      top      = \boxinnersep,
      bottom   = \boxinnersep,
      title    = {\textbf{Beispiel}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = beispielframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = beispielframe,
        colframe = beispielframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = beispielcolor,
      colframe     = beispielframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{Beispiel: \lzbeispieltitle}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = beispielframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = beispielframe,
        colframe = beispielframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 10.  aufgabe[Titel]
%%      Aufgaben-Box mit automatischer Nummerierung (Goldgelb).
%%      Zähler zurücksetzen: \resetaufgaben
%%
%%  Verwendung:
%%    \begin{aufgabe}[Laufzeitanalyse]
%%      Bestimmen Sie die Zeitkomplexität von Bubblesort.
%%    \end{aufgabe}
%% ══════════════════════════════════════════════════════════════════════════
\newcounter{aufgabectr}
\newcommand{\resetaufgaben}{\setcounter{aufgabectr}{0}}

\NewDocumentEnvironment{aufgabe}{O{}}{%
  \stepcounter{aufgabectr}%
  \def\lzaufgabetitle{#1}%
  \ifdefempty{\lzaufgabetitle}{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = aufgabecolor,
      colframe     = aufgabeframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{Aufgabe~\theaufgabectr}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = aufgabeframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = aufgabeframe,
        colframe = aufgabeframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }{%
    \begin{tcolorbox}[
      enhanced, breakable,
      colback      = aufgabecolor,
      colframe     = aufgabeframe,
      boxrule      = 1pt,
      arc          = 4pt,
      left         = \boxinnersep,
      right        = \boxinnersep,
      top          = \boxinnersep,
      bottom       = \boxinnersep,
      title        = {\textbf{Aufgabe~\theaufgabectr: \lzaufgabetitle}},
      fonttitle    = \bfseries\color{white},
      colbacktitle = aufgabeframe,
      attach boxed title to top left = {yshift=-2mm, xshift=4mm},
      boxed title style = {
        colback  = aufgabeframe,
        colframe = aufgabeframe,
        arc = 3pt, top = 2pt, bottom = 2pt,
      },
    ]%
  }%
}{%
  \end{tcolorbox}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 11.  konzeptkarte{Titel}  +  \kzweispaltig{links}{rechts}
%%      Strukturierte Übersichtskarte. Innen können alle anderen Umgebungen
%%      und \kzweispaltig für ein Zwei-Spalten-Layout verwendet werden.
%%
%%  Verwendung:
%%    \begin{konzeptkarte}{Quicksort}
%%      Kurze Beschreibung des Algorithmus.
%%      \kzweispaltig{
%%        \begin{stdtable}{l X}{Eigenschaft & Wert}
%%          Stabil & Nein \\
%%        \end{stdtable}
%%      }{
%%        \begin{process}
%%          \step{Pivot wählen}
%%          \step{Partitionieren}
%%        \end{process}
%%      }
%%    \end{konzeptkarte}
%% ══════════════════════════════════════════════════════════════════════════
\newenvironment{konzeptkarte}[1]{%
  \begin{tcolorbox}[
    enhanced,
    breakable,
    title        = {\sffamily\bfseries\large #1},
    colback      = konzeptcolor,
    colframe     = primary!35!white,
    colbacktitle = primary,
    coltitle     = white,
    boxrule      = 0.8pt,
    arc          = 5pt,
    left         = 10pt,
    right        = 10pt,
    top          = 8pt,
    bottom       = 8pt,
    fonttitle    = \sffamily\bfseries,
  ]%
}{%
  \end{tcolorbox}%
}

\newcommand{\kzweispaltig}[2]{%
  \par\smallskip\noindent
  \begin{minipage}[t]{0.475\linewidth}
    #1
  \end{minipage}%
  \hfill
  \begin{minipage}[t]{0.475\linewidth}
    #2
  \end{minipage}%
  \par\smallskip
}

%% ══════════════════════════════════════════════════════════════════════════
%% 12.  \icode{text}
%%      Inline-Code mit grauem Hintergrund. Für Sonderzeichen (\_ usw.)
%%      müssen diese wie in normalem LaTeX-Text escaped werden.
%%
%%  Verwendung:
%%    Die Funktion \icode{quicksort(arr)} gibt ein sortiertes Array zurück.
%% ══════════════════════════════════════════════════════════════════════════
\newcommand{\icode}[1]{%
  \mbox{\colorbox{codeblock}{\texttt{\small\color{primary!85!black}\strut#1}}}%
}

%% ══════════════════════════════════════════════════════════════════════════
%% 13.  syntaxbox[Sprache]
%%      Code-Block mit Syntax-Highlighting. Standard: Python.
%%      Unterstützte Sprachen: Python, Java, C, C++, SQL, bash, …
%%      (alle lstlisting-Sprachen, siehe listings-Dokumentation)
%%
%%  Verwendung:
%%    \begin{syntaxbox}[Java]
%%      public int factorial(int n) {
%%          return n <= 1 ? 1 : n * factorial(n - 1);
%%      }
%%    \end{syntaxbox}
%% ══════════════════════════════════════════════════════════════════════════
\tcbuselibrary{listings}

\lstdefinestyle{lzsyntax}{%
  basicstyle        = \ttfamily\small,
  keywordstyle      = \color{accent}\bfseries,
  commentstyle      = \color{primary!55!white}\itshape,
  stringstyle       = \color{proframe},
  numberstyle       = \tiny\color{primary!40!white},
  numbers           = none,
  breaklines        = true,
  showstringspaces  = false,
  tabsize           = 2,
  frame             = none,
  keepspaces        = true,
  columns           = fullflexible,
}

\newtcblisting{syntaxbox}[1][Python]{%
  enhanced,
  breakable,
  colback  = codeblock,
  colframe = codeframe,
  boxrule  = 0.5pt,
  arc      = 3pt,
  left     = 8pt,
  right    = 8pt,
  top      = 6pt,
  bottom   = 6pt,
  listing only,
  listing options = {style=lzsyntax, language=#1},
}
